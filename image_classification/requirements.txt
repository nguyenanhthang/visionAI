# Core deep-learning stack. TensorFlow ships Keras 3 as `keras`.
# For CPU-only machines you can install `tensorflow-cpu` instead of `tensorflow`.
tensorflow>=2.16,<2.22

# Numerics, plotting, metrics, image IO and config parsing.
numpy>=1.26
matplotlib>=3.7
scikit-learn>=1.3
Pillow>=10.0
PyYAML>=6.0
