PySide6>=6.5.0
opencv-python>=4.8.0
# numpy <2: cv2/torch/paddlepaddle thường build cho numpy 1.x. Cài numpy 2.x
# gây lỗi ABI: "module compiled against ABI version 0x1000009 but this version
# of numpy is 0x2000000" → "numpy.core.multiarray failed to import".
# Nếu lỡ bị nâng (do cài paddle/easyocr): pip install "numpy<2"
numpy>=1.24.0,<2
Pillow>=10.0.0
# ── Optional — OCR Max tool (chọn 1 trong 3 engine, hoặc nhiều cho 'auto') ──
# LƯU Ý: nhiều engine OCR kéo theo numpy 2.x khi cài → sau khi cài, chạy lại
#        pip install "numpy<2" để cv2 không vỡ ABI.
#
# 1) PaddleOCR — chính xác cao cho hoá đơn/chứng từ + tiếng Việt:
#      pip install "paddleocr==2.7.3" "paddlepaddle==2.6.2"  (combo ổn định, KHÔNG PIR)
#    (PaddleOCR 3.x cũng chạy; nếu gặp lỗi PIR 'ConvertPirAttribute…' → dùng combo 2.x trên.)
# paddleocr==2.7.3
# paddlepaddle==2.6.2
#
# 2) Tesseract — nhẹ, cần Tesseract binary + langpack hệ thống:
#      Ubuntu:  sudo apt install tesseract-ocr tesseract-ocr-vie tesseract-ocr-eng
#      Windows: https://github.com/UB-Mannheim/tesseract/wiki
#      macOS:   brew install tesseract tesseract-lang
# pytesseract>=0.3.10
#
# 3) EasyOCR — không cần binary, tốt cho tiếng Việt + nền phức tạp,
#    nhưng nặng (PyTorch ~500MB+, load model lần đầu ~5-15s):
# easyocr>=1.7.0
#
# ── Optional — Barcode/QR (ID Reader tool) ──
# pyzbar>=0.1.9
