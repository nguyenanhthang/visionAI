PySide6>=6.5.0
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0
# ── Optional — OCR Max tool (chọn 1 trong 2 engine, hoặc cả 2 cho 'auto') ──
# 1) Tesseract — nhẹ, cần Tesseract binary + langpack hệ thống:
#      Ubuntu:  sudo apt install tesseract-ocr tesseract-ocr-vie tesseract-ocr-eng
#      Windows: https://github.com/UB-Mannheim/tesseract/wiki
#      macOS:   brew install tesseract tesseract-lang
# pytesseract>=0.3.10
#
# 2) EasyOCR — không cần binary, tốt cho tiếng Việt + nền phức tạp,
#    nhưng nặng (PyTorch ~500MB+, load model lần đầu ~5-15s):
# easyocr>=1.7.0
#
# ── Optional — Barcode/QR (ID Reader tool) ──
# pyzbar>=0.1.9
#
# ── Optional — YOLO Detect (train + fast CPU inference) ──
# ultralytics cho train + .pt fallback (YOLO Studio cần).
# onnxruntime giúp inference .onnx nhanh hơn PyTorch CPU ~3-4x ở FP32
# (lossless — accuracy giữ nguyên). Không cài thì auto-fallback cv2.dnn
# (đã có sẵn qua opencv-python), vẫn nhanh hơn .pt khoảng 2-3x.
# ultralytics>=8.0.0
# onnxruntime>=1.16.0
# onnx>=1.12.0,<2.0.0     # core ONNX + đọc class names từ metadata
# onnxslim>=0.1.71        # simplify graph khi export (ultralytics yêu cầu)
#
# ► Để bấm "Export ONNX" trong YOLO Studio không phải đợi ultralytics
#   AutoUpdate (download chậm + đôi khi fail), cài trước cả 3 package:
#     pip install onnx>=1.12.0 "onnx<2.0.0" onnxslim>=0.1.71 onnxruntime
